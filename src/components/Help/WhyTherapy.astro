---
import { getEntry, render } from 'astro:content'
import type { CollectionEntry } from 'astro:content'

const helpLanding: CollectionEntry<"help"> | undefined = await getEntry("help", "why_therapy")
if (!helpLanding) {
  throw new Error('Could not find help\'s "why_therapy" text')
}

const { Content } = await render(helpLanding)

const title: string = "Why Attending Therapy?"
---

<section
  class="container-fluid col-sm-10 col-md-9 min-vh-100 d-flex flex-column justify-content-center py-5"
  aria-labelledby="why-therapy-title"
>
  <h2 class="my-5 mt-2 text-center" id="why-therapy-title">
    {title}
  </h2>
  <div class="container-fluid">
    <Content />
  </div>
</section>